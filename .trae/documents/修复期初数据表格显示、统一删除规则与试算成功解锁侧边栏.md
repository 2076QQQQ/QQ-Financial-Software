## 问题与目标
- 1) 期初数据页的表格仍未显示：需要确保路由、表格组件与内容容器的滚动设置正确，页面能渲染各类别科目行。
- 2) 统一删除规则：所有模块（科目、资金账户、账套等）删除都必须先停用，且未被使用；前端按钮需禁用并给出提示，后端接口需强校验。
- 3) 保存并试算平衡后，侧边栏应立即解锁（不必再点“确认完成期初初始化”）。

## 修复方案
### 一、期初数据表格显示
- 验证并修复：
  - 路由 `pages/settings/initial-data.tsx` 正确导出 `setup/InitialDataEntry`；表格使用的 `ui/table.tsx` 已渲染 children；主内容容器 `MainLayout` 已设置 `overflow-x-auto`。
  - 若仍空白，补充内层容器 `overflow-x-auto` 并在 `InitialDataEntry` 的表格外包裹宽度容器，确保列超出时可横向滚动。

### 二、删除规则统一（必须停用 + 未使用）
- 科目（Subjects）：
  - 前端：在 `SubjectManagement.tsx` 将删除按钮的可用条件改为：非内置、无余额、无子科目、且已停用。
  - 后端：在 `DELETE /api/settings/subjects` 增加判断 `isActive === false`，否则返回 403。
- 资金账户（Fund Accounts）：
  - 后端模型补充 `status` 字段；
    - `POST /api/settings/fund-accounts` 接受并保存 `status`；
    - `PUT` 接受并更新 `status`；
    - `DELETE` 要求 `status === '停用'`（并保留已被引用不可删的规则）。
  - 前端：删除按钮需同时满足未被引用且 `status === '停用'`，并给出禁用提示。
- 账套（Account Books）：
  - 已有“未启用且无记录才能删除”，保持一致；前端禁用规则已实现。

### 三、试算成功自动解锁侧边栏
- 在 `InitialDataEntry.tsx` 的 `handleSaveAndCheck`：
  - 后端试算返回成功后，自动调用 `POST /api/settings/initialization/complete`；
  - 刷新首页或直接跳转首页，`MainLayout` 根据 `has_initial_balances=true` 解锁侧边栏。

## 涉及文件
- 前端：
  - `frontend/pages/setup/InitialDataEntry.tsx`
  - `frontend/pages/settings/SubjectManagement.tsx`
  - `frontend/pages/settings/FundAccountManagement.tsx`
  - （必要时）在期初数据页表格外增加 `overflow-x-auto` 容器
- 后端：
  - `backend/server.ts`（完善 Fund Accounts 的 `POST/PUT/DELETE`、Subjects 的 `DELETE` 判断）

## 验证
- 进入期初数据页，表格完整显示各列；需要时可横向滚动。
- 在科目/资金账户列表中，未停用或已被使用的项删除按钮禁用并提示；停用后可删除且后端校验通过。
- 保存并试算平衡后立即解锁侧边栏（首页向导消失），可访问其它模块。

请确认上述方案，我将按此实施并验证效果。