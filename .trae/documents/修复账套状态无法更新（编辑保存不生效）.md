## 问题诊断
- 在编辑弹窗中切换为“已停用”并保存后，列表仍显示“已启用”。
- 原因：`AccountBookModal` 的提交数据未包含 `isActive` 字段，后端 `PUT /api/settings/account-books` 虽支持更新状态，但前端没有把状态传过去。
- 证据：
  - 前端提交位置：`frontend/pages/settings/AccountBookModal.tsx:164-169` 仅发送 `name/startPeriod/review_enabled`，缺失 `isActive`。
  - 后端更新逻辑：`backend/server.ts:195-215` 会合并 `updates`，若包含 `isActive` 则持久化。

## 修复方案
1. 在 `AccountBookModal` 的 `handleSubmit` 中把 `isActive` 一并纳入提交 payload：
   - 数据结构示例：`{ name, startPeriod, requiresAudit, isActive }`
   - 其中 `requiresAudit` 或现有的 `review_enabled` 均可，建议统一为 `requiresAudit`（后端会映射为 `review_enabled`）。
2. 保留现有 `handleSave` 的 `PUT` 逻辑（`frontend/pages/settings/AccountBookList.tsx:123-151`），继续传入 `id` 以定位账套。
3. 保存成功后调用已有的 `fetchAccountBooks` 刷新列表，确保状态在 UI 中更新（已存在于代码中）。
4. 验证：
   - 打开某个账套编辑弹窗，切换为“已停用”，点击保存；
   - 列表应显示该账套的状态徽章为“未启用”。

## 代码改动要点
- 文件：`frontend/pages/settings/AccountBookModal.tsx`
- 在 `handleSubmit` 中构造的 `dataToSave` 增加 `isActive` 与（建议）`requiresAudit` 键。
- 不改变表单与校验逻辑，仅扩充提交字段。

## 兼容与安全
- 后端已支持 `isActive` 持久化，无需改动；
- 若仅修改状态，不变更日期，`selectedYear/Month/Day` 已由现有账套解析赋值，`startPeriod` 保持原值；
- 前端刷新机制已就位，不需要新增状态管理。

请确认以上方案，我将立即按此实施并验证效果。