## 问题定位
- 列表不显示：前端 `AccountBookList.tsx` 的 useEffect 中未调用后端加载接口（注释掉了），且后端缺少 GET `/api/settings/account-books` 列表路由。
- 创建后不呈现：保存时仅 POST 创建并立即 `router.push('/')`，列表未刷新；再次进入页面也无法显示，因为没有 GET 端点或未调用。
- 页面不填满：右侧内容区已改为 `min-h-screen flex flex-col`，但账套页本身顶层容器没有占满剩余空间，表格容器也未声明可伸展。

## 后端改动（server.ts）
- 添加 GET `/api/settings/account-books`：返回当前公司所有账套列表（按 `companyId` 过滤）。
- 保持现有 POST `/api/settings/account-books`（已实现）与只在启动时 `initDb()`（已修复）。

## 前端改动
- `AccountBookList.tsx`
  - 在 `useEffect` 中调用 GET `/api/settings/account-books` 并 `setAccountBooks`。
  - 在 `handleSave` 成功后：
    - 两种选项：
      1) 刷新列表并停留在当前页（立即 `fetch` 填充列表，避免用户感觉“没保存”）；
      2) 跳转到首页工作台 `/Dashboard/Dashboard` 展示设置清单（推荐，不经过 `/`）。
  - 页面结构：
    - 顶层容器改为 `min-h-screen flex flex-col`；
    - 表格外层卡片加入 `flex-1` 以填满剩余空间；
    - 保持欢迎弹窗与“新增账套”逻辑。
- 可选：若首页仍通过 `/` 跳转，确保不要用服务端重定向到登录；建议跳转到 `/Dashboard/Dashboard`，由 RouteGuard 做分流。

## 验证
- 创建账套后，列表立即显示新项（选择“留在页面”）或跳转到首页显示清单（选择“跳转至首页”）。
- 退出后重新登录，`has_account_book=true` 持久；列表 GET 能正确返回数据；页面高度填满，卡片不再“残缺”。

## 说明
- 所有 API 都基于会话解析 `userId/companyId` 并按公司维度过滤数据；不会跨公司串数据。
- 不变更端口：前端 3000，后端 3001；前端代理到后端照常工作。