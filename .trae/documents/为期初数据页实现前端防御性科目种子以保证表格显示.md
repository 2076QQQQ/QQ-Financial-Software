## 问题
- 刷新后 mockData 清空，InitialDataEntry 调用 getAllSubjects() 返回空数组，表格没有任何行。

## 方案
- 在 `frontend/pages/setup/InitialDataEntry.tsx` 内实现防御逻辑：
  - 初始化阶段（构造 `subjects` 初始 state）读取 `getAllSubjects()`；若为空，自动生成一组内置基础科目（1001/1002/1003/1005/1008、2002/2003、3001/3103、5001、6001/6602/6902/6901）并通过 `setSubjects(...)` 写入 mockData。
  - 生成的科目包含必要字段：`id/code/name/category/direction/isActive/isBuiltIn/hasBalance/hasChildren/level`。
  - 然后按原逻辑构造 `initialSubjects`，表格即可渲染。
- 保持已修复的滚动容器（`overflow-x-auto`）以确保列完整显示。

## 不改变内容
- 保持“试算成功弹窗”原始确认流程；不自动完成初始化。

## 验证
- 刷新后进入期初数据页，表格在五个类别下均能显示基础科目行；如科目本已存在，则不重复生成。

我将按此修改 `InitialDataEntry.tsx` 并验证表格显示。